#include "main.h"
/**
 *  _abc -> return absolute value of number
 *  @n: parameter
 *  Return: absolute value of a number
 */
int _abs(int n)
{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else 
	return (c * -1);
}
