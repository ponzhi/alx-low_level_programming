#include <stdio.h>

/**
 * main - Prints the name of the file of the program
 * was compilied from. followed by a new line.
 * Return: Alwayss 0.
 */

int main(void)
{
	printf("%s\n", _FILE_);

	return (0);
}
