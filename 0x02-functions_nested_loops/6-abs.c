#include "main.h"
/**
 *  _abc -> return absolute value of number
 *  @n: parameter
 *  Return: absolute value of a number
 */
int _abs(int n)
{
	if (n > 0 || n == 0)
	{
	return (n);
	}
	else 
	return (n * -1);
}
